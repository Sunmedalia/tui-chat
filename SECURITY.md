# 安全策略

## 支持范围

安全修复只面向最新发布版本和 `main` 分支。旧版本可能继续运行，但不会单独回补安全补丁。

## 私下报告漏洞

优先使用 GitHub 仓库的 **Security → Report a vulnerability**（Private Vulnerability Reporting，入口可见时）提交报告。如果该入口尚未启用，只创建一个不含利用细节的 Issue 请求私密联系方式。报告中请包含受影响版本、复现步骤、预期影响，以及在安全环境中得到的必要日志；请删除账号密码、本地存储口令、私钥、完整密文数据库和真实聊天内容。

维护者会尽快确认报告。在修复发布前，请不要公开漏洞细节或对非自有服务器执行测试。

## 威胁模型与边界

- 客户端之间的聊天正文由端到端加密保护，服务器只保存路由元数据和密文。
- TLS、设备签名、账号主密钥签发的设备证书和安全码校验共同降低中间人攻击风险。
- 服务端仍可观察账号、设备、连接时间、来源 IP、通信双方和密文大小等元数据；本项目不提供匿名通信。
- 已解锁的终端、被入侵的客户端或服务端管理员终端不在端到端加密能够完全保护的范围内。
- 本地数据库密钥由本地存储口令保护。遗忘口令无法恢复；弱口令、终端录屏、键盘记录器和交换分区可能泄露明文。
- 首次联系或设备密钥变化后必须人工核对安全码。忽略 `/verify` 提示会失去身份验证保证。

## 部署要求

- 公网只暴露 TLS 反向代理的 `wss://.../v2/ws`，不要直接暴露 Rust 服务端口或管理 Unix Socket。
- 必须将 `trusted_proxy_cidrs` 限制为实际反向代理地址；不要信任任意 `X-Forwarded-*` 请求头。
- 定期备份 `/data/server.db`，保护备份权限，并测试恢复流程。
- 及时更新镜像摘要、Rust 依赖和 GitHub Actions 固定提交；发布资产应核验 SHA-256 和 GitHub provenance attestation。
- 根据磁盘容量设置账号/全局密文配额和最小空闲空间，并监控 `storage status`。
